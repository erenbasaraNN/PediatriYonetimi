@model IEnumerable<PediatriYonetimi.Models.Duyuru>

<h2> Duyuru Listesi</h2>
<a asp-action="DuyuruEkle" class="btn btn-success mb-2">Duyuru Ekle</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Başlık</th>
            <th>Açıklama</th>
            <th>Tarih</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var duyuru in Model)
        {
            <tr>
                <td>@duyuru.Baslik</td>
                <td>@duyuru.Icerik</td>
                <td>@duyuru.YayinTarihi.ToString("g")</td>
                <td>
                    <a asp-action="DuyuruSil" asp-route-id="@duyuru.Id" class="btn btn-danger btn-sm"
                       onclick="return confirm('Bu duyuruyu silmek istediğinize emin misiniz?');">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
